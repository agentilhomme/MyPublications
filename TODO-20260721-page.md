# Spec: Publications page with citation charts

**Status:** draft
**Owner:** Anais Gentilhomme
**Created:** 2026-07-21

## Goal

Publish a live web page listing my papers, each with a small chart showing
citations per year, so it can be shared as a public portfolio/CV page.

## Author identity note

The ORCID iD I was given (`0009-0000-0235-5559`) has a public profile but
**no linked works** and no matching OpenAlex author record. Data below was
instead found by searching OpenAlex by name and picking the best match:

- Name searched: "Anais Gentilhomme" / "Anais S. Gentilhomme"
- Matched author: **Anais S. Gentilhomme** — OpenAlex ID [`A5009459257`](https://openalex.org/A5009459257)
- This author record has no ORCID attached either, and there were 3 other
  similarly-named OpenAlex authors with 1 work each that were *not* used
  (lower confidence, likely different people).
- **Action needed:** confirm this is the correct author identity before
  publishing publicly. If it's wrong, or if the real ORCID should be
  linked, update the ORCID record or re-run the fetch script with a
  corrected `--search`/`--author-id`/`--orcid` value.

## Data

Fetched via [scripts/fetch_publications.py](scripts/fetch_publications.py)
(reusable — re-run any time to refresh) and saved to
[data/publications.json](data/publications.json).

Fields captured per paper: title, venue, year, work type, a link (DOI or
landing page), total citation count, and citations broken down by year.

### Papers found (3)

| Year | Title | Venue | Type | Total cites |
|---|---|---|---|---|
| 2025 | Proteomic Insights into Psychrophile Growth in Perchlorate-Amended Subzero Conditions: Implications for Martian Life Detection | Astrobiology | article | 2 |
| 2024 | Genomic signatures of cold adaptation in the family Colwelliaceae | Extremophiles | article | 2 |
| 2016 | Mitochondrial and plastid genome analysis of the heteromorphic red alga *Mastocarpus papillatus* ... | Mitochondrial DNA Part B | article | 11 |

Full per-year citation breakdowns are in `data/publications.json`.

## Known data quirks to handle when building the page

- The 2016 title contains raw `<i>...</i>` HTML (species name italics) from
  OpenAlex — needs safe rendering, not literal display of tags.
- Citation-by-year data is sparse (only years with >0 citations are
  present) — the page should fill in zero-citation years for a continuous
  chart axis rather than skipping them.
- OpenAlex `counts_by_year` typically only covers the last ~10 years; the
  2016 paper's earliest years may be missing/zero as a result.

## TODO

- [ ] Confirm author identity match above (or correct it) before anything
      is published live.
- [ ] Decide on page tech/hosting (e.g. static HTML + GitHub Pages, since
      this repo is already public on GitHub).
- [ ] Build a page-generation script (e.g.
      `scripts/build_page.py`) that reads `data/publications.json` and
      renders an HTML page: one entry per paper (title, venue, year, type,
      link) plus a small per-year citation bar/line chart.
- [ ] Fill zero-citation years in the chart data before rendering.
- [ ] Set up GitHub Pages (or chosen host) for live publishing.
- [ ] Re-run `scripts/fetch_publications.py` periodically to refresh
      citation counts.
